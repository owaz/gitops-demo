apiVersion: v1
kind: Service
metadata:
  name: prd
  namespace: alpha
  labels:
    app: prd
    team: alpha
spec:
  type: ClusterIP
  selector:
    app: prd
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
