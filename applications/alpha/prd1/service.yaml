apiVersion: v1
kind: Service
metadata:
  name: prd1
  namespace: alpha
  labels:
    app: prd1
    team: alpha
spec:
  type: ClusterIP
  selector:
    app: prd1
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
