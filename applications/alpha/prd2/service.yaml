apiVersion: v1
kind: Service
metadata:
  name: prd2
  namespace: alpha
  labels:
    app: prd2
    team: alpha
spec:
  type: ClusterIP
  selector:
    app: prd2
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
