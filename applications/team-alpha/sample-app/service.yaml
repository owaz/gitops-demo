apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: team-alpha
  labels:
    app: sample-app
    team: alpha
spec:
  type: ClusterIP
  selector:
    app: sample-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
